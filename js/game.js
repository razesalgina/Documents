// js/game.js
(function () {
  function initGamePage() {
    // TODO:
    // - Baca parameter match dari URL (mis. ?matchId=123)
    // - Ambil daftar game dari storage berdasarkan matchId
    // - Render baris game ke tabel
  }

  document.addEventListener('DOMContentLoaded', () => {
    initGamePage();
  });
})();